import SwiftUI

struct TicketsGridView: View {
    var body: some View {
        Text("Tickets Grid View")
    }
}

struct TicketsGridView_Previews: PreviewProvider {
    static var previews: some View {
        TicketsGridView()
    }
}
